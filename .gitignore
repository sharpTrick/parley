# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Test / coverage
coverage/

# SQLite scratch DBs (tests + local runs)
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite-*

# Parley local read-state (per-instance cursor store)
.parley-state/
read-state.json

# Secrets — backend_config / .env live here, NEVER committed (DESIGN §14)
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
*.log
.idea/
.vscode/
