{
  "name": "@parley/conformance",
  "version": "0.1.0",
  "type": "module",
  "description": "Parley shared conformance suite: write once against the seam, run against every backend plugin.",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@parley/core": "*"
  },
  "peerDependencies": {
    "vitest": "^2"
  }
}
