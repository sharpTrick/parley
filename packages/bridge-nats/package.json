{
  "name": "@parley/nats",
  "version": "0.1.0",
  "type": "module",
  "description": "@parley/nats — subject->topic, JetStream seq->cursor, wildcard->subscribe. Skeleton against the frozen Parley seam — planned v0.5.",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@parley/core": "*"
  },
  "devDependencies": {
    "@parley/conformance": "*"
  },
  "keywords": ["mcp", "parley", "nats", "message-bus", "agent-messaging"]
}
