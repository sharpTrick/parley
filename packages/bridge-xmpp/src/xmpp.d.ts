/**
 * `@xmpp/client` ships no type declarations and there is no `@types` package, so the plugin uses its
 * runtime API (`client`/`xml`) untyped and declares the module here to keep the build clean.
 */
declare module '@xmpp/client';
