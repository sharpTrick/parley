{
  "name": "@parley/xmpp",
  "version": "0.1.0",
  "type": "module",
  "description": "@parley/xmpp — MUC->topic, MAM->fetchRecent/cursor (MAM must be enabled), PubSub->subscribe. Skeleton against the frozen Parley seam — planned v0.5.",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@parley/core": "*",
    "@xmpp/client": "^0.14.0",
    "@xmpp/debug": "^0.14.0"
  },
  "devDependencies": {
    "@parley/conformance": "*"
  },
  "keywords": [
    "mcp",
    "parley",
    "xmpp",
    "message-bus",
    "agent-messaging"
  ]
}
